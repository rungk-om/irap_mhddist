[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "irap_mhdist"
version = "0.1.0"
description = "MHD distribution function library"
readme = "README.md"
license = {text = "MIT"}
authors = [
  { name="Richard Hitier", email="hitier.richard@gmail.com" }
]
dependencies = []

